from setuptools import setup, find_packages

# This file is needed for compatibility with tools that don't yet 
# fully support PEP 621 project metadata from pyproject.toml

setup(
    name="spotify-advance",
    version="0.1.0",
    packages=find_packages(),
) 